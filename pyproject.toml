[project]
name = "aeo-kvm"
version = "0.2.0"
description = "Switch keyboard, video, and mouse between hosts"

# Note: This is a Bun/TypeScript project. pyproject.toml is used for version metadata only.
# Build with: make build (uses Bun compiler)
